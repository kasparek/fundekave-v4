MIGRATION:
delete FROM `sys_pages_items` where typeId='' or typeId is null;
update sys_pages set galeryDir='page/event' where pageId='event';
update sys_pages set galeryDir=concat('page/',pageId) where typeId='blog' or typeId='forum';
update sys_pages_items set text = concat(text,"<br /><br />\n",enclosure) where typeId='forum' and enclosure is not null;
update sys_pages_items set text = concat(text,"<br /><br />\n<a href=\"http://fundekave.net/?i=",itemIdBottom,"\">Odkaz</a>") where (itemIdBottom is not null && itemIdBottom != 0);
update sys_pages_items set text = concat(text,"<br /><br />\n<a href=\"http://fundekave.net/?k=",pageIdBottom,"\">Odkaz</a>") where (pageIdBottom is not null && pageIdBottom != 0);
update sys_pages_items set text=concat(text,'<br/> ',enclosure),enclosure=null where typeId='forum' and enclosure is not null and enclosure != '';
ALTER TABLE `sys_pages_items` DROP `itemIdBottom`,CHANGE `pageIdBottom` `pageIdTop` varchar(5) NULL AFTER `pageId`;
ALTER TABLE `sys_users` DROP `skinId`,DROP `zbanner`,DROP `zavatar`,DROP `zforumico`,DROP `zgalerytype`;
ALTER TABLE `sys_users_friends` DROP `comment`;
DROP sys_menu_secondary;
ALTER TABLE `sys_pages` DROP `menuSecondaryGroup`,DROP `nameshort`,DROP `authorContent`,DROP `pageParams`;
update sys_pages set template='page_PageItemList' where typeId in ('forum','blog','galery');

optimize:
user ALTER TABLE...ORDER BY?

FIX:
drop draft manualy
profil upload - zalozit galerii, prdet na limit, schovat si funkce, treba budu chtit limit jindy
-tlacitko pouzit na avatar
nefungje pridat galerii, klub, blog
tlacitka se zobrazuji neprihlasenym - profil, pridat atd
fuup - mezera kdyz neni checkbox, vetsi fontsize
upload - image/profil
vkladani kratkyho commentu do blogu zustava neprecteny
nastaveni - category edit - remove pager
posta messages - deleting - del[] nefunguje v ajaxu
sidebar category list
mainbar category filtering - category to header
handle itemVo->pageVo - lazy loading

TODO:
-merge logged users with login - show on click

-testovat - galery fullscreen
-fullscreen - current item(will be useful for random slidshow)/next/prev/exit button/play/pause(simulate click on item every 5sec)

-testovat - posta/chat

-zpravy predelat na jako chat
	na titulni pravidelne kontrolovat na novy zpravy
	v poste pravidelne kontrolovat a stahovat novy zpravy
	kdyz se chatuje a najednou je vic zprav nez na stranku, updatovat pager

- galery otestovat ftp nahravani
- clean up - kesovani vseho, projit stranky
- osobni - opravit nahravani - novy adresar - test
- osobni - otestovat ftp nahravani
- events - nahravani letaku - novy adresat, novy url na thumnail, neni potreba generovat - test

-sidebar kalendar - ?date=2010-10-10 - odkaz na zive s omezenim na urcity den - omezeni na den funguje
-tagy
 - stranka s nahodnyma fotkama
 - u kazdy fotky ikonka na tagovani
 - v pratelich zebricek tagovani	
TAGS-PALCE -nej stranka - podle palcu za obdobi,podle zobrazeni za odbobi TODO: history update
-udalosti - vlastni,opakujici se,upozorneni

FUVATAR
fuvatar - pridat u ikonky ze je online, odkaz na stranku se vsema kamerama, pripojena diskuze

SIDEBAR
2. customizace levyho sloupce pro stranky
STRANKY
3. vytvareni novych stranek
